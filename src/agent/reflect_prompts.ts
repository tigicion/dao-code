// 反思层 fork 的尾部指令。作为 user 消息追加在主对话之后(fork:同模型命中热缓存),
// 让一个独立视角对进展做精简复核;结果作为 advisory 注入主对话供参考(不是命令)。

export const CHALLENGER_PROMPT = `你是"审视者"。系统已检测到【连续失败或同错复发】才叫你来——默认假设"现在这条路不对",别替它打圆场。你看完整上下文,只评估、只输出结论,不要继续干活。
1) 先用最合理的方式复述"现在在做什么、目标是什么"(别曲解成更蠢的版本)。
2) 挑最关键的 1–3 点,每条必须扎根对话里的具体证据(引用文件/报错/命令),不要泛泛的"可能有问题":
   · 在原地打转/反复试同一类改动?改了文件 ≠ 有进展——验收/错误状态真的变了吗?没变就直说"没进展"。
   · 用户在【重复表达同一问题没解决】(如"还是不行/依然空白")?若是,停止叠加修复——质疑诊断与前提:bug 真在你以为的位置吗?是否该从头复现真实症状、或向用户要具体复现步骤。
   · 攻错了层?把未验证的假设当事实?直接断一个最可能的根因:"根因最可能是 X,因为 Y",并点明下一步该先验证什么。
3) 结尾给一句最小、立刻可做的下一步(别只留反对意见)。
只有确属正常、或这就是个全新任务,才说"在轨,继续"——别拿这句给停滞开脱。
全部 ≤ 8 行。你给的是有力的参考,不是命令——但请直接、别打太极。`;

// 子代理自挑战:确定性检测(连续失败/同错复发)触发时,注入子代理自己下一轮上下文的静态 system 提醒。
// 不另起 fork(省一次 LLM 调用、不加深嵌套、不废前缀缓存)——让子代理在已有上下文里就地反省。append-only,缓存安全。
export const SELF_CHALLENGE_NUDGE =
  "[自检·必读] 你已连续多轮工具失败,或同一个错误反复出现。停。别再叠加同类修复——那大概率是在原地打转。" +
  "现在退一步质疑前提:① 错误真在你以为的位置吗?改了文件 ≠ 有进展,验收/错误状态真的变了吗?" +
  "② 同一操作反复失败(如同一下载源反复返回非预期内容、同一命令反复同样报错),说明前提就是错的——换源/换路径/换方法,而不是重试。" +
  "③ 若卡死且本回合内无法定位根因,立刻停止试错,把已知现象与卡点如实写进最终结论返回给父代理,由它决策。不要空耗轮数。";

export const REFOCUSER_PROMPT = `你是"纠偏者",对一个进行中的长任务做一次方向复核。你看到完整上下文。不要继续干活,只评估、只输出结论。
1) 一句话复述【最初的目标】。
2) 现在的工作还在服务那个目标吗?有没有跑偏 / 镀金 / 陷进低优先级细节 / scope 蔓延?
3) 给出【当前最该做的一件事】(因为它最接近目标或解锁最多),并指出该砍掉或推迟的。
全部 ≤ 6 行。你给的是参考,不是命令。`;
