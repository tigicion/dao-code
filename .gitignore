node_modules/
dist/
dist-bin/
/dao
*.log
.env
.env.local
.env.*.local
.DS_Store
# .dao/ 内容默认忽略,但项目级权限配置 settings.json 需随仓库共享(对标 CC:settings.json 入库、settings.local.json 不入库)
.dao/*
!.dao/settings.json
.codeds/
.claude/
evals/report.md
evals/runs/
evals/terminal-bench/runs/
__pycache__/
*.pyc

# 私有项目指令(不提交)
DAO.local.md

# 框架 wire 快照里含真实 ~/.dao 记忆的两份(system-prompt 把 {memory} 段也拼进去了),不提交公开仓库;
# 其余 wire 文件(tools/skills/subagents/middleware)不含个人数据,可入库。
# 用 `npm run debug:harness-wire` 本地重新生成全部。
docs/harness/wire/memory.wire.md
docs/harness/wire/system-prompt.wire.md
